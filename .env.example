# Copy to .env and fill in. Loaded via python-dotenv by the CLI.
# Get a development key at https://developer.riotgames.com/ (expires every 24h;
# apply for a production key for sustained crawling).
RIOT_API_KEY=RGAPI-your-key-here
